---
type MediaImage = {
	src: string;
	alt: string;
	width: number;
	height: number;
};

type Props = {
	image: MediaImage;
	dialogId?: string;
	label?: string;
};

const { image, dialogId, label = `Ampliar imagen: ${image.alt}` } = Astro.props;
const isInteractive = Boolean(dialogId);
---

<div class="project-media" style={`aspect-ratio: ${image.width} / ${image.height}`}>
	{isInteractive ? (
		<button
			class="media-trigger"
			type="button"
			data-gallery-open
			aria-controls={dialogId}
			aria-haspopup="dialog"
			aria-label={label}
		>
			<img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" />
		</button>
	) : (
		<img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" />
	)}
</div>

<style>
	.project-media {
		width: 100%;
		overflow: hidden;
		background: var(--surface-2);
	}

	.project-media img {
		width: 100%;
		height: 100%;
		object-fit: contain;
		object-position: center;
	}

	.media-trigger {
		display: block;
		width: 100%;
		height: 100%;
		padding: 0;
		border: 0;
		background: transparent;
		cursor: zoom-in;
	}
</style>
